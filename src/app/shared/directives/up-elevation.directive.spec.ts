import { UpElevationDirective } from './up-elevation.directive';

describe('UpElevationDirective', () => {
  it('should create an instance', () => {
    const directive = new UpElevationDirective();
    expect(directive).toBeTruthy();
  });
});
