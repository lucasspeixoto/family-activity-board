/* tslint:disable:no-unused-variable */
import { TypeFilterPipe } from './type-filter.pipe';

describe('Pipe: TypeFiltere', () => {
  it('create an instance', () => {
    const pipe = new TypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
