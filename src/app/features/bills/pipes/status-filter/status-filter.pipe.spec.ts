/* tslint:disable:no-unused-variable */
import { StatusFilterPipe } from './status-filter.pipe';

describe('Pipe: StatusFiltere', () => {
  it('create an instance', () => {
    const pipe = new StatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
